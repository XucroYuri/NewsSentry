# Schema: ../../schemas/sourcechannel.schema.json
# 契约: docs/contracts-canonical.md §2
# ADR: ADR-0015
source_id: chinafile
display_name: "ChinaFile"
type: rss
url: "https://feeds.feedburner.com/chinafile/All"

# credibility_base: 0-100 量纲中的基础可信度，详见 contracts-canonical.md §4
credibility_base: 0.80

fetch_interval_minutes: 45
max_items_per_run: 40
timeout_seconds: 30
enabled: true

health:
  last_success_at: null
  consecutive_failures: 0
