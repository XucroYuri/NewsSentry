# Phase 49.5b: 前端视觉翻新 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 News Sentry 前端从功能性暗色 UI 升级为专业产品级视觉体验，包括设计系统升级、登录页品牌化、事件卡片层级重构、骨架屏系统和响应式布局。

**Architecture:** 纯 CSS + Vanilla JS 改造，零新依赖。设计系统通过 CSS 变量扩展实现；骨架屏用 CSS `@keyframes` + 占位 HTML；响应式用 `@media` 断点。所有改动限于 `src/news_sentry/static/` 目录。

**Tech Stack:** CSS Custom Properties / Vanilla JS ES modules / CSS `@keyframes` / `@media` queries

**Spec:** `docs/specs/2026-05-18-phase-49.5-app-productization-design.md` §4

---

## 文件变更地图

| 文件 | 职责 | 变更类型 |
|------|------|---------|
| `src/news_sentry/static/style.css:7-47` | CSS 变量根 | 修改 — 新增 6 个设计 token |
| `src/news_sentry/static/style.css:302-323` | Loading/spinner | 修改 — 新增骨架屏 keyframes + 类 |
| `src/news_sentry/static/style.css:348-384` | 统计卡片 | 修改 — 升级为双层阴影 + 玻璃态 |
| `src/news_sentry/static/style.css:600-651` | 事件卡片 | 修改 — L1/L2/L3 三级视觉层级 |
| `src/news_sentry/static/style.css:1773-1875` | 登录页 | 修改 — 品牌化重构 + 模式切换 |
| `src/news_sentry/static/style.css:939-976` | 响应式断点 | 修改 — 三断点体系 |
| `src/news_sentry/static/index.html:13-48` | 登录页 HTML | 修改 — 模式切换 + icon 前缀 |
| `src/news_sentry/static/app.js:240-289` | 连接逻辑 | 修改 — 模式切换 + 服务器状态检测 |
| `src/news_sentry/static/pages/events.js:314-335` | 事件卡片渲染 | 修改 — L1/L2/L3 层级重构 |
| `src/news_sentry/static/pages/dashboard.js:42-93` | 概览 Tab | 修改 — 骨架屏替换 spinner |
| `src/news_sentry/static/pages/events.js:87-93` | 事件列表 | 修改 — 骨架屏替换 spinner |

---

### Task 1: 设计系统升级 (CSS 变量 + 排版层级 + 圆角/阴影)

**Files:**
- Modify: `src/news_sentry/static/style.css:7-47` (CSS 变量)
- Modify: `src/news_sentry/static/style.css:335-384` (卡片 + 统计卡片样式)

- [ ] **Step 1: 新增设计系统 CSS 变量**

在 `style.css:47`（`:root` 闭合大括号前）插入以下变量：

```css
  /* ── Phase 49.5b: 设计系统升级 ── */
  --bg-glass: rgba(26, 29, 39, 0.7);
  --bg-card-hover: #252a3b;
  --accent-glow: 0 0 20px rgba(79, 143, 247, 0.15);
  --text-title: #f0f1f5;
  --radius-xl: 18px;
  --shadow-card: 0 2px 8px rgba(0, 0, 0, 0.3), 0 8px 32px rgba(0, 0, 0, 0.2);

  /* 排版层级 */
  --font-h1: 1.75rem;
  --font-h2: 1.25rem;
  --font-h3: 1rem;
  --font-body: 0.875rem;
  --font-caption: 0.75rem;
  --weight-h1: 700;
  --weight-h2: 600;
  --weight-h3: 600;
  --weight-body: 400;
  --weight-caption: 400;
```

- [ ] **Step 2: 升级基础卡片样式**

将 `style.css:336-345` 的 `.card` 替换为：

```css
.card {
  background: var(--bg-glass);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-lg);
  padding: 20px;
  box-shadow: var(--shadow-card);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  transition: border-color var(--transition-fast), box-shadow var(--transition-fast);
}
.card:hover {
  border-color: var(--border-light);
  box-shadow: var(--shadow-card), var(--accent-glow);
}
```

- [ ] **Step 3: 升级统计卡片样式**

将 `style.css:355-384` 的 `.stat-card` 替换为：

```css
.stat-card {
  background: var(--bg-glass);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-lg);
  padding: 20px;
  box-shadow: var(--shadow-card);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  transition: all var(--transition-fast);
}
.stat-card:hover {
  border-color: var(--accent-blue);
  box-shadow: var(--shadow-card), var(--accent-glow);
  background: var(--bg-card-hover);
}

.stat-card .stat-label {
  font-size: var(--font-caption);
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: 8px;
  font-weight: var(--weight-caption);
}

.stat-card .stat-value {
  font-size: 1.8rem;
  font-weight: var(--weight-h1);
  line-height: 1.1;
  color: var(--text-title);
}

.stat-card .stat-value.accent-blue { color: var(--accent-blue); }
.stat-card .stat-value.accent-green { color: var(--accent-green); }
.stat-card .stat-value.accent-orange { color: var(--accent-orange); }
```

- [ ] **Step 4: 新增排版层级工具类**

在 `.card:hover` 规则之后（约 `style.css:346` 附近）插入：

```css
/* ── 排版层级 ─────────────────────────────────────────── */
.h1 { font-size: var(--font-h1); font-weight: var(--weight-h1); color: var(--text-title); }
.h2 { font-size: var(--font-h2); font-weight: var(--weight-h2); color: var(--text-primary); }
.h3 { font-size: var(--font-h3); font-weight: var(--weight-h3); color: var(--text-primary); }
.body-text { font-size: var(--font-body); font-weight: var(--weight-body); color: var(--text-primary); }
.caption { font-size: var(--font-caption); font-weight: var(--weight-caption); color: var(--text-muted); }
```

- [ ] **Step 5: 验证 CSS 无语法错误**

```bash
node -e "
const fs = require('fs');
const css = fs.readFileSync('src/news_sentry/static/style.css', 'utf8');
const opens = (css.match(/{/g) || []).length;
const closes = (css.match(/}/g) || []).length;
if (opens !== closes) { console.error('Brace mismatch: ' + opens + ' opens vs ' + closes + ' closes'); process.exit(1); }
console.log('CSS brace check passed: ' + opens + ' pairs');
"
```

Expected: `CSS brace check passed: N pairs`

- [ ] **Step 6: 运行全量测试确保无回归**

```bash
.venv/bin/python3 -m pytest tests/ -q 2>&1 | tail -5
```

Expected: 1640+ tests passed.

- [ ] **Step 7: Commit**

```bash
git add src/news_sentry/static/style.css
git commit -m "feat: Phase 49.5b Task 1 — 设计系统升级 (CSS 变量 + 排版层级 + 玻璃态卡片)"

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

### Task 2: 登录页品牌化重构

**Files:**
- Modify: `src/news_sentry/static/index.html:13-48` (登录页 HTML)
- Modify: `src/news_sentry/static/app.js:240-289` (连接逻辑 + 模式切换 + 服务器状态)
- Modify: `src/news_sentry/static/style.css:1773-1875` (登录页样式)

- [ ] **Step 1: 重构登录页 HTML**

将 `index.html:13-48` 的 `connectPage` div 替换为：

```html
  <!-- 连接设置页（未认证时全屏显示） -->
  <div id="connectPage" class="connect-page">
    <div class="connect-card">
      <!-- 模式切换标签 -->
      <div class="connect-mode-switch">
        <button class="mode-tab active" data-mode="local">本地服务</button>
        <button class="mode-tab" data-mode="cloud">云端服务</button>
      </div>

      <!-- Logo -->
      <div class="connect-logo">
        <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/>
          <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/>
        </svg>
      </div>
      <h1 class="connect-title">News Sentry</h1>
      <p class="connect-subtitle">新闻智能监控平台</p>

      <!-- 表单 -->
      <div class="connect-form">
        <div class="connect-field">
          <label>👤 用户名</label>
          <input type="text" id="connectUsername" placeholder="输入用户名" autocomplete="username">
        </div>
        <div class="connect-field">
          <label>🌐 服务器地址</label>
          <input type="url" id="connectServer" value="http://localhost:8000">
        </div>
        <div class="connect-field">
          <label>🔒 密码</label>
          <input type="password" id="connectPassword" placeholder="输入密码" autocomplete="current-password">
        </div>
        <div class="connect-field">
          <label>🌍 Language / 语言</label>
          <select id="connectLanguage">
            <option value="zh">中文</option>
            <option value="en">English</option>
          </select>
        </div>
        <button id="connectBtn" class="connect-btn">登录</button>
        <p class="connect-hint">首次使用？请使用管理员账户登录</p>
        <p id="connectError" class="connect-error" style="display:none;"></p>
      </div>

      <!-- 服务器状态 -->
      <div class="connect-status" id="connectStatus">
        <span class="status-dot" id="connectStatusDot"></span>
        <span id="connectStatusText">检测中...</span>
      </div>
    </div>
  </div>
```

- [ ] **Step 2: 更新登录页 CSS**

将 `style.css:1773-1875` 的 `.connect-page` 到 `.connect-error` 替换为：

```css
.connect-page {
  position: fixed;
  inset: 0;
  z-index: 9999;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #0a0d13 0%, #0f1117 40%, #161b22 100%);
}

.connect-card {
  background: var(--bg-glass);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-xl);
  padding: 40px 36px 32px;
  width: 100%;
  max-width: 400px;
  text-align: center;
  box-shadow: var(--shadow-card);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
}

/* 模式切换 */
.connect-mode-switch {
  display: flex;
  background: var(--bg-tertiary);
  border-radius: var(--radius-sm);
  padding: 3px;
  margin-bottom: 28px;
}

.mode-tab {
  flex: 1;
  padding: 8px 12px;
  background: transparent;
  border: none;
  border-radius: 4px;
  color: var(--text-muted);
  font-size: 0.8rem;
  font-weight: 500;
  cursor: pointer;
  transition: all var(--transition-fast);
}

.mode-tab.active {
  background: var(--accent-blue);
  color: white;
  box-shadow: 0 2px 8px rgba(79, 143, 247, 0.3);
}

.connect-logo {
  margin-bottom: 12px;
  color: var(--accent-blue);
}

.connect-title {
  font-size: var(--font-h1);
  font-weight: var(--weight-h1);
  color: var(--text-title);
  margin: 0 0 4px;
}

.connect-subtitle {
  font-size: var(--font-caption);
  color: var(--text-secondary);
  margin: 0 0 28px;
}

.connect-form {
  text-align: left;
}

.connect-field {
  margin-bottom: 14px;
}

.connect-field label {
  display: block;
  font-size: 0.8rem;
  font-weight: 500;
  color: var(--text-secondary);
  margin-bottom: 6px;
}

.connect-field input,
.connect-field select {
  width: 100%;
  padding: 10px 12px;
  background: var(--bg-primary);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-sm);
  color: var(--text-primary);
  font-size: 0.875rem;
  box-sizing: border-box;
  transition: border-color var(--transition-fast), box-shadow var(--transition-fast);
}

.connect-field input:focus,
.connect-field select:focus {
  outline: none;
  border-color: var(--accent-blue);
  box-shadow: 0 0 0 3px var(--accent-blue-dim);
}

.connect-btn {
  width: 100%;
  padding: 12px;
  background: linear-gradient(135deg, var(--accent-blue), #3a7bd5);
  color: white;
  border: none;
  border-radius: var(--radius-sm);
  font-size: 0.95rem;
  font-weight: 600;
  cursor: pointer;
  transition: all var(--transition-fast);
  margin-top: 8px;
}

.connect-btn:hover { box-shadow: 0 4px 16px rgba(79, 143, 247, 0.3); }
.connect-btn:disabled { opacity: 0.5; cursor: not-allowed; }

.connect-hint {
  font-size: 0.75rem;
  color: var(--text-muted);
  margin: 16px 0 0;
  text-align: center;
}

.connect-error {
  color: var(--accent-red);
  font-size: 0.8rem;
  margin: 12px 0 0;
  text-align: center;
}

/* 服务器状态 */
.connect-status {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  margin-top: 20px;
  padding-top: 16px;
  border-top: 1px solid var(--border-color);
  font-size: 0.75rem;
  color: var(--text-muted);
}

.connect-status .status-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--text-muted);
  flex-shrink: 0;
}

.connect-status .status-dot.ok { background: var(--accent-green); }
.connect-status .status-dot.error { background: var(--accent-red); }
```

- [ ] **Step 3: 更新 app.js 连接逻辑 — 模式切换 + 服务器状态检测**

在 `app.js` 的 `showConnectPage()` 函数（约 L240-249）替换为：

```javascript
function showConnectPage() {
  const cp = document.getElementById("connectPage");
  if (cp) cp.style.display = "flex";

  // 恢复保存的连接或使用默认值
  const conn = getConnection();
  const serverInput = document.getElementById("connectServer");
  if (conn?.server && serverInput) {
    serverInput.value = conn.server;
    // 根据保存的 URL 判断模式
    const isLocal = conn.server.includes("localhost") || conn.server.includes("127.0.0.1");
    setConnectMode(isLocal ? "local" : "cloud");
  } else {
    setConnectMode("local");
  }

  // 检测服务器状态
  checkServerStatus();
}

/** 设置连接模式 (local/cloud) */
function setConnectMode(mode) {
  const tabs = document.querySelectorAll(".mode-tab");
  tabs.forEach(tab => tab.classList.toggle("active", tab.dataset.mode === mode));

  const serverInput = document.getElementById("connectServer");
  if (!serverInput) return;

  if (mode === "local") {
    if (!serverInput.value.includes("localhost") && !serverInput.value.includes("127.0.0.1")) {
      serverInput.value = "http://localhost:8000";
    }
  } else {
    if (serverInput.value.includes("localhost") || serverInput.value.includes("127.0.0.1")) {
      serverInput.value = "https://news-sentry.xuyu.workers.dev";
    }
  }
  checkServerStatus();
}

/** 检测服务器状态 */
async function checkServerStatus() {
  const dot = document.getElementById("connectStatusDot");
  const text = document.getElementById("connectStatusText");
  const server = document.getElementById("connectServer")?.value?.trim();
  if (!dot || !text || !server) return;

  dot.className = "status-dot";
  text.textContent = "检测中...";

  try {
    const controller = new AbortController();
    const timeout = setTimeout(() => controller.abort(), 5000);
    const resp = await fetch(`${server}/api/v1/health`, { signal: controller.signal });
    clearTimeout(timeout);
    if (resp.ok) {
      dot.className = "status-dot ok";
      text.textContent = "服务器在线";
    } else {
      dot.className = "status-dot error";
      text.textContent = "服务器响应异常";
    }
  } catch {
    dot.className = "status-dot error";
    text.textContent = "无法连接服务器";
  }
}
```

- [ ] **Step 4: 在 init() 中绑定模式切换和服务器状态检测事件**

在 `app.js` 的 `init()` 函数中，`connectBtn` 事件绑定之后（约 L507 后）插入：

```javascript
  // 模式切换标签
  document.querySelectorAll(".mode-tab").forEach(tab => {
    tab.addEventListener("click", () => setConnectMode(tab.dataset.mode));
  });

  // 服务器地址变化时重新检测
  const serverInput = document.getElementById("connectServer");
  if (serverInput) {
    let serverCheckTimer = null;
    serverInput.addEventListener("input", () => {
      clearTimeout(serverCheckTimer);
      serverCheckTimer = setTimeout(checkServerStatus, 600);
    });
  }
```

- [ ] **Step 5: 验证 JS 无语法错误**

```bash
node -e "import('./src/news_sentry/static/app.js').catch(e => { if (e.message.includes('SyntaxError')) { console.error(e.message); process.exit(1) } else { console.log('Import skipped (expected in browser context)') } })" 2>&1
```

Expected: 无 SyntaxError。

- [ ] **Step 6: 验证 CSS 无语法错误**

```bash
node -e "
const fs = require('fs');
const css = fs.readFileSync('src/news_sentry/static/style.css', 'utf8');
const opens = (css.match(/{/g) || []).length;
const closes = (css.match(/}/g) || []).length;
if (opens !== closes) { console.error('Brace mismatch: ' + opens + ' opens vs ' + closes + ' closes'); process.exit(1); }
console.log('CSS brace check passed: ' + opens + ' pairs');
"
```

Expected: `CSS brace check passed: N pairs`

- [ ] **Step 7: 运行全量测试**

```bash
.venv/bin/python3 -m pytest tests/ -q 2>&1 | tail -5
```

Expected: 1640+ tests passed.

- [ ] **Step 8: Commit**

```bash
git add src/news_sentry/static/index.html src/news_sentry/static/app.js src/news_sentry/static/style.css
git commit -m "feat: Phase 49.5b Task 2 — 登录页品牌化重构 (模式切换 + 服务器状态检测 + 玻璃态)"

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

### Task 3: 事件卡片视觉层级重构

**Files:**
- Modify: `src/news_sentry/static/pages/events.js:314-335` (事件卡片渲染)
- Modify: `src/news_sentry/static/style.css:600-651` (事件卡片样式)

- [ ] **Step 1: 重构事件卡片 CSS**

将 `style.css:600-651` 的 `.event-card` 到 `.event-card-scores` 替换为：

```css
.event-card {
  background: var(--bg-glass);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-lg);
  padding: 20px 24px;
  cursor: pointer;
  transition: all var(--transition-fast);
  box-shadow: var(--shadow-card);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  animation: slideUp 0.3s ease both;
}
.event-card:hover {
  border-color: var(--accent-blue);
  box-shadow: var(--shadow-card), var(--accent-glow);
  transform: translateY(-2px);
}

/* L1: 标题 + 分数 — 最高视觉权重 */
.event-card-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 16px;
  margin-bottom: 8px;
}

.event-card-title {
  font-size: var(--font-body);
  font-weight: var(--weight-h3);
  color: var(--text-title);
  line-height: 1.5;
  flex: 1;
}

.event-card-score-badge {
  font-size: var(--font-body);
  font-weight: var(--weight-h1);
  color: var(--accent-blue);
  flex-shrink: 0;
  min-width: 36px;
  text-align: right;
  font-family: var(--font-mono);
}

.event-card-time {
  font-size: var(--font-caption);
  color: var(--text-muted);
  white-space: nowrap;
  font-family: var(--font-mono);
  flex-shrink: 0;
}

/* L2: 来源 + 时间 + 分类 — 中等视觉权重 */
.event-card-meta {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 8px;
  margin-bottom: 12px;
  font-size: var(--font-caption);
  color: var(--text-secondary);
}

.event-card-meta .tag-source {
  background: var(--accent-blue-dim);
  color: var(--accent-blue);
  padding: 2px 8px;
  border-radius: 4px;
  font-size: 0.7rem;
  font-weight: 500;
}

.event-card-meta .tag-classification {
  background: var(--bg-tertiary);
  color: var(--text-secondary);
  padding: 2px 8px;
  border-radius: 4px;
  font-size: 0.7rem;
}

/* L3: 分数条 + 实体标签 — 最低视觉权重，移动端隐藏 */
.event-card-scores {
  display: flex;
  gap: 20px;
  flex-wrap: wrap;
  padding-top: 10px;
  border-top: 1px solid var(--border-color);
}

.event-card-entities {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin-top: 8px;
}
```

- [ ] **Step 2: 重构事件卡片渲染 HTML**

在 `events.js` 的 `loadEventList()` 函数中（约 L314-335），替换事件卡片 HTML 模板：

将：
```javascript
    const listHtml = events
      .map(
        (ev, i) => `
      <div class="event-card" data-event-id="${escapeHtml(ev.id || "")}" style="animation-delay:${i * 40}ms">
        <div class="event-card-header">
          <div class="event-card-title">${sentimentDotHtml(ev.sentiment)}${escapeHtml(ev.title_original || ev.id || "无标题")}</div>
          <div class="event-card-time">${formatDate(ev.published_at)}</div>
        </div>
        <div class="event-card-meta">
          <span class="tag tag-source">${escapeHtml(ev.source_id || "—")}</span>
          ${ev.classification?.l0 ? `<span class="tag tag-classification">${escapeHtml(ev.classification.l0)}</span>` : ""}
        </div>
        <div class="event-card-scores">
          ${scoreBar("新闻价值", ev.news_value_score)}
          ${scoreBar("中国相关度", ev.china_relevance)}
        </div>
        ${ev.nlp_entities ? entityChipsHtml(ev.nlp_entities) : ""}
      </div>
    `
      )
      .join("");
```

替换为：
```javascript
    const listHtml = events
      .map(
        (ev, i) => {
          const score = ev.news_value_score ?? 0;
          const scoreColor = score >= 80 ? "var(--accent-orange)" : score >= 60 ? "var(--accent-blue)" : "var(--text-muted)";
          return `
      <div class="event-card" data-event-id="${escapeHtml(ev.id || "")}" style="animation-delay:${i * 40}ms">
        <div class="event-card-header">
          <div class="event-card-title">${escapeHtml(ev.title_original || ev.id || "无标题")}</div>
          <div class="event-card-score-badge" style="color:${scoreColor}">${score}</div>
        </div>
        <div class="event-card-meta">
          ${sentimentDotHtml(ev.sentiment)}
          <span class="tag-source">${escapeHtml(ev.source_id || "—")}</span>
          ${ev.classification?.l0 ? `<span class="tag-classification">${escapeHtml(ev.classification.l0)}</span>` : ""}
          <span>${formatDate(ev.published_at)}</span>
        </div>
        <div class="event-card-scores">
          ${scoreBar("新闻价值", ev.news_value_score)}
          ${scoreBar("中国相关度", ev.china_relevance)}
        </div>
        ${ev.nlp_entities ? `<div class="event-card-entities">${entityChipsHtml(ev.nlp_entities)}</div>` : ""}
      </div>
    `;
        }
      )
      .join("");
```

- [ ] **Step 3: 验证 JS 无语法错误**

```bash
node -e "import('./src/news_sentry/static/pages/events.js').catch(e => { if (e.message.includes('SyntaxError')) { console.error(e.message); process.exit(1) } else { console.log('Import skipped (expected in browser context)') } })" 2>&1
```

Expected: 无 SyntaxError。

- [ ] **Step 4: 验证 CSS brace 平衡**

```bash
node -e "
const fs = require('fs');
const css = fs.readFileSync('src/news_sentry/static/style.css', 'utf8');
const opens = (css.match(/{/g) || []).length;
const closes = (css.match(/}/g) || []).length;
if (opens !== closes) { console.error('Brace mismatch: ' + opens + ' opens vs ' + closes + ' closes'); process.exit(1); }
console.log('CSS brace check passed: ' + opens + ' pairs');
"
```

Expected: `CSS brace check passed: N pairs`

- [ ] **Step 5: 运行全量测试**

```bash
.venv/bin/python3 -m pytest tests/ -q 2>&1 | tail -5
```

Expected: 1640+ tests passed.

- [ ] **Step 6: Commit**

```bash
git add src/news_sentry/static/pages/events.js src/news_sentry/static/style.css
git commit -m "feat: Phase 49.5b Task 3 — 事件卡片 L1/L2/L3 视觉层级重构"

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

### Task 4: 骨架屏系统 + 引导式空状态统一

**Files:**
- Modify: `src/news_sentry/static/style.css:302-323` (loading/spinner → 骨架屏)
- Modify: `src/news_sentry/static/pages/dashboard.js:42-93` (概览 Tab 骨架屏)
- Modify: `src/news_sentry/static/pages/events.js:87-93,302-311` (事件列表骨架屏 + 空状态)
- Modify: `src/news_sentry/static/api.js` (新增 emptyStateHtml 导出)

- [ ] **Step 1: 新增骨架屏 CSS**

在 `style.css` 的 `@keyframes spin` 规则之后（约 L323），插入骨架屏样式：

```css
/* ── 骨架屏 ─────────────────────────────────────────── */

@keyframes skeleton-pulse {
  0%, 100% { opacity: 0.4; }
  50% { opacity: 0.8; }
}

.skeleton {
  background: var(--bg-tertiary);
  border-radius: var(--radius-sm);
  animation: skeleton-pulse 1.5s ease-in-out infinite;
}

/* 统计卡片骨架 */
.skeleton-stat-cards {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 16px;
  margin-bottom: 28px;
}

.skeleton-stat {
  background: var(--bg-secondary);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-lg);
  padding: 20px;
}

.skeleton-stat .skeleton-label {
  width: 60%;
  height: 12px;
  margin-bottom: 14px;
}

.skeleton-stat .skeleton-value {
  width: 40%;
  height: 28px;
}

/* 事件卡片骨架 */
.skeleton-event-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.skeleton-event {
  background: var(--bg-secondary);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-md);
  padding: 18px 22px;
}

.skeleton-event .skeleton-title {
  width: 75%;
  height: 16px;
  margin-bottom: 10px;
}

.skeleton-event .skeleton-meta {
  width: 45%;
  height: 12px;
  margin-bottom: 12px;
}

.skeleton-event .skeleton-bar {
  width: 60%;
  height: 8px;
}
```

- [ ] **Step 2: 在 api.js 中新增 `emptyStateHtml` 工具函数**

在 `api.js` 的导出函数区域（找到 `export function showSuccess` 等附近），添加：

```javascript
/**
 * 生成引导式空状态 HTML。
 * @param {string} icon - emoji icon
 * @param {string} title - 标题文字
 * @param {string} description - 描述文字
 * @param {Array<{label:string, href?:string, id?:string, primary?:boolean}>} actions - 操作按钮
 */
export function emptyStateHtml(icon, title, description, actions = []) {
  const actionsHtml = actions.map(a => {
    const cls = a.primary ? "btn-primary" : "btn-secondary";
    if (a.href) return `<a href="${a.href}" class="${cls}">${a.label}</a>`;
    if (a.id) return `<button class="${cls}" id="${a.id}">${a.label}</button>`;
    return `<button class="${cls}">${a.label}</button>`;
  }).join("");

  return `
    <div class="empty-state-guided">
      <div class="empty-state-icon">${icon}</div>
      <h2 class="empty-state-title">${escapeHtml(title)}</h2>
      <p class="empty-state-causes">${escapeHtml(description)}</p>
      ${actionsHtml ? `<div class="empty-state-actions">${actionsHtml}</div>` : ""}
    </div>
  `;
}
```

确保 `escapeHtml` 已在同一文件中定义并在此函数之前可用（当前 api.js 已导出 `escapeHtml`，确认其定义在 `emptyStateHtml` 之前）。

- [ ] **Step 3: 在 dashboard.js 概览 Tab 中使用骨架屏**

在 `dashboard.js` 的 `renderOverviewTab()` 函数中（约 L66-67），将 `dashTopEvents` 区域的加载指示器：

```html
<div id="dashTopEvents">
  <div class="loading-spinner"><div class="spinner"></div><p>正在加载...</p></div>
</div>
```

替换为：

```html
<div id="dashTopEvents">
  <div class="skeleton-event-list">
    <div class="skeleton-event"><div class="skeleton skeleton-title"></div><div class="skeleton skeleton-meta"></div><div class="skeleton skeleton-bar"></div></div>
    <div class="skeleton-event"><div class="skeleton skeleton-title"></div><div class="skeleton skeleton-meta"></div><div class="skeleton skeleton-bar"></div></div>
    <div class="skeleton-event"><div class="skeleton skeleton-title"></div><div class="skeleton skeleton-meta"></div><div class="skeleton skeleton-bar"></div></div>
  </div>
</div>
```

同样将统计卡片区域（约 L52-57）的初始内容改为骨架屏：

```html
<div class="stat-cards" id="dashStatCards">
  <div class="skeleton-stat-cards">
    <div class="skeleton-stat"><div class="skeleton skeleton-label"></div><div class="skeleton skeleton-value"></div></div>
    <div class="skeleton-stat"><div class="skeleton skeleton-label"></div><div class="skeleton skeleton-value"></div></div>
    <div class="skeleton-stat"><div class="skeleton skeleton-label"></div><div class="skeleton skeleton-value"></div></div>
    <div class="skeleton-stat"><div class="skeleton skeleton-label"></div><div class="skeleton skeleton-value"></div></div>
  </div>
</div>
```

- [ ] **Step 4: 在 events.js 事件列表中使用骨架屏**

在 `events.js` 的 `loadEventList()` 函数中（约 L278），将：

```javascript
area.innerHTML = '<div class="loading-spinner"><div class="spinner"></div><p>正在加载事件...</p></div>';
```

替换为：

```javascript
area.innerHTML = `
  <div class="skeleton-event-list">
    ${Array.from({length: 6}, () => '<div class="skeleton-event"><div class="skeleton skeleton-title"></div><div class="skeleton skeleton-meta"></div><div class="skeleton skeleton-bar"></div></div>').join("")}
  </div>
`;
```

同样，在 `renderEventsTab()` 中（约 L91），将：

```javascript
<div class="loading-spinner"><div class="spinner"></div><p>正在加载事件...</p></div>
```

替换为：

```html
<div class="skeleton-event-list">
  <div class="skeleton-event"><div class="skeleton skeleton-title"></div><div class="skeleton skeleton-meta"></div><div class="skeleton skeleton-bar"></div></div>
  <div class="skeleton-event"><div class="skeleton skeleton-title"></div><div class="skeleton skeleton-meta"></div><div class="skeleton skeleton-bar"></div></div>
  <div class="skeleton-event"><div class="skeleton skeleton-title"></div><div class="skeleton skeleton-meta"></div><div class="skeleton skeleton-bar"></div></div>
</div>
```

- [ ] **Step 5: 统一事件列表空状态**

在 `events.js` 的 `loadEventList()` 中（约 L302-311），将空白空状态：

```javascript
if (events.length === 0) {
  area.innerHTML = `
    <div class="empty-state">
      <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/>
      </svg>
      <p>暂无匹配的事件</p>
    </div>
  `;
  return;
}
```

替换为使用 `emptyStateHtml`：

```javascript
if (events.length === 0) {
  area.innerHTML = emptyStateHtml(
    "📰",
    "暂无匹配的事件",
    "尝试调整筛选条件，或等待自动采集器完成下一轮采集",
    [{ label: "清除筛选", id: "clearFilters" }, { label: "查看诊断", href: "#/ops/collector" }]
  );
  const clearBtn = area.querySelector("#clearFilters");
  if (clearBtn) {
    clearBtn.addEventListener("click", () => {
      state.filters = { source_id: "", classification: "", min_score: 0, search: "", page: 1, sentiment: "", entity: "", topic_tag: "", date_from: "", date_to: "" };
      renderEventsTab(container);
    });
  }
  return;
}
```

同时需要在 `events.js` 顶部 import 中添加 `emptyStateHtml`：

```javascript
import { state, api, apiPost, escapeHtml, showError, showSuccess, formatDate, scoreColor, scoreGradient, scoreBar, sentimentColor, sentimentPct, sentimentGradient, sentimentLabelColor, sentimentDotHtml, entityChipsHtml, copyToClipboard, logAction, emptyStateHtml } from "../api.js";
```

- [ ] **Step 6: 验证 JS 无语法错误**

```bash
node -e "import('./src/news_sentry/static/api.js').catch(e => { if (e.message.includes('SyntaxError')) { console.error(e.message); process.exit(1) } else { console.log('Import skipped') } })" 2>&1
node -e "import('./src/news_sentry/static/pages/events.js').catch(e => { if (e.message.includes('SyntaxError')) { console.error(e.message); process.exit(1) } else { console.log('Import skipped') } })" 2>&1
node -e "import('./src/news_sentry/static/pages/dashboard.js').catch(e => { if (e.message.includes('SyntaxError')) { console.error(e.message); process.exit(1) } else { console.log('Import skipped') } })" 2>&1
```

Expected: 无 SyntaxError。

- [ ] **Step 7: 运行全量测试**

```bash
.venv/bin/python3 -m pytest tests/ -q 2>&1 | tail -5
```

Expected: 1640+ tests passed.

- [ ] **Step 8: Commit**

```bash
git add src/news_sentry/static/style.css src/news_sentry/static/api.js src/news_sentry/static/pages/dashboard.js src/news_sentry/static/pages/events.js
git commit -m "feat: Phase 49.5b Task 4 — 骨架屏系统 + 引导式空状态统一"

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

### Task 5: 响应式布局 (Tablet 768-1023px + Mobile <768px)

**Files:**
- Modify: `src/news_sentry/static/style.css:939-976` (响应式断点)
- Modify: `src/news_sentry/static/style.css:2341-2363` (现有 768px 断点)

- [ ] **Step 1: 重构响应式断点体系**

将 `style.css:939-976` 和 `style.css:2341-2363` 的两段 `@media` 规则统一替换。首先找到并删除现有的两个 `@media` 块：

1. `@media (max-width: 768px)` 块（约 L939 附近，包含 `.page-container`、`.top-bar`、`.dashboard-grid`、`.stat-cards` 等）
2. `@media (max-width: 768px)` 块（约 L2341 附近，包含 `.connect-card`、`.tab-bar`、`.tab-item` 等）
3. `@media (min-width: 769px) and (max-width: 1100px)` 块（约 L972 附近）

在文件末尾（`style.css` 最后一行之后）新增统一的三断点响应式：

```css
/* ============================================================
   响应式布局 — 三断点体系
   Desktop ≥1024px | Tablet 768-1023px | Mobile <768px
   ============================================================ */

/* ── Tablet (768-1023px) ─────────────────────────────── */
@media (min-width: 768px) and (max-width: 1023px) {
  :root {
    --sidebar-width: 0px;
  }

  .sidebar {
    transform: translateX(-100%);
    position: fixed;
    z-index: 100;
    transition: transform var(--transition-normal);
  }

  .sidebar.open {
    transform: translateX(0);
  }

  .main-content {
    margin-left: 0;
  }

  .hamburger-btn {
    display: flex;
  }

  .dashboard-grid {
    grid-template-columns: 1fr;
  }

  .stat-cards {
    grid-template-columns: repeat(2, 1fr);
  }

  .connect-card {
    margin: 16px;
    padding: 32px 28px;
  }
}

/* ── Mobile (<768px) ─────────────────────────────────── */
@media (max-width: 767px) {
  :root {
    --sidebar-width: 0px;
    --topbar-height: 48px;
  }

  .sidebar {
    transform: translateX(-100%);
    position: fixed;
    z-index: 100;
    width: 260px;
    transition: transform var(--transition-normal);
  }

  .sidebar.open {
    transform: translateX(0);
  }

  .main-content {
    margin-left: 0;
  }

  .hamburger-btn {
    display: flex;
  }

  /* Top bar */
  .top-bar {
    padding: 0 12px;
    height: var(--topbar-height);
  }

  .target-selector select {
    max-width: 120px;
    font-size: 0.8rem;
  }

  /* Tab bar */
  .tab-bar {
    padding: 0 8px;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
  }

  .tab-item {
    padding: 8px 12px;
    font-size: 0.8rem;
    white-space: nowrap;
  }

  /* Page container */
  .page-container {
    padding: 16px 12px;
  }

  /* Stat cards: stack */
  .stat-cards {
    grid-template-columns: 1fr 1fr;
    gap: 10px;
  }

  .stat-card {
    padding: 14px;
  }

  .stat-card .stat-value {
    font-size: 1.4rem;
  }

  /* Event cards: hide L3 on mobile */
  .event-card {
    padding: 14px 16px;
  }

  .event-card-scores,
  .event-card-entities {
    display: none;
  }

  .event-card-header {
    flex-direction: column;
    gap: 4px;
  }

  .event-card-title {
    font-size: 0.875rem;
  }

  /* Dashboard grid */
  .dashboard-grid {
    grid-template-columns: 1fr;
  }

  /* Filter bar */
  .filter-bar {
    flex-direction: column;
    align-items: stretch;
  }

  .filter-group select,
  .filter-group input[type="search"],
  .filter-group input[type="text"] {
    min-width: auto;
    width: 100%;
  }

  .filter-group input[type="range"] {
    width: 100%;
  }

  /* Detail page */
  .detail-header {
    padding: 16px 14px 14px;
  }

  .detail-body {
    padding: 14px;
  }

  .detail-score-grid {
    grid-template-columns: 1fr;
  }

  .detail-field {
    grid-template-columns: 1fr;
    gap: 2px;
  }

  /* Pipeline stages */
  .pipeline-stages {
    flex-direction: column;
  }

  .stage-arrow {
    transform: rotate(90deg);
  }

  /* Connect page */
  .connect-card {
    margin: 12px;
    padding: 28px 20px;
  }

  .connect-mode-switch {
    margin-bottom: 20px;
  }

  /* Bar chart */
  .bar-chart-item {
    grid-template-columns: 80px 1fr 36px;
    gap: 8px;
  }

  /* Skeleton adjustments */
  .skeleton-stat-cards {
    grid-template-columns: 1fr 1fr;
  }
}
```

- [ ] **Step 2: 确认旧的 `@media` 块已被删除**

搜索确认不存在重复的 `@media (max-width: 768px)` 块：

```bash
grep -n "@media" src/news_sentry/static/style.css
```

Expected: 只有新的三断点 `@media` 规则（768-1023px Tablet 和 <768px Mobile），无旧的重复块。

- [ ] **Step 3: 验证 CSS brace 平衡**

```bash
node -e "
const fs = require('fs');
const css = fs.readFileSync('src/news_sentry/static/style.css', 'utf8');
const opens = (css.match(/{/g) || []).length;
const closes = (css.match(/}/g) || []).length;
if (opens !== closes) { console.error('Brace mismatch: ' + opens + ' opens vs ' + closes + ' closes'); process.exit(1); }
console.log('CSS brace check passed: ' + opens + ' pairs');
"
```

Expected: `CSS brace check passed: N pairs`

- [ ] **Step 4: 运行全量测试**

```bash
.venv/bin/python3 -m pytest tests/ -q 2>&1 | tail -5
```

Expected: 1640+ tests passed.

- [ ] **Step 5: Commit**

```bash
git add src/news_sentry/static/style.css
git commit -m "feat: Phase 49.5b Task 5 — 响应式布局三断点体系 (Desktop/Tablet/Mobile)"

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

## 验证清单

完成全部 5 个 Task 后执行：

1. 登录页显示模式切换标签（"本地服务"/"云端服务"），切换时自动变更服务器地址
2. 登录页输入地址后自动检测 `/health`，显示"服务器在线"或"无法连接服务器"
3. 登录页使用玻璃态卡片 + 渐变背景 + 蓝色登录按钮（非绿色）
4. 统计卡片使用玻璃态 + 双层阴影 + hover 发光效果
5. 事件卡片信息层级清晰：L1 标题+分数 → L2 来源+时间+分类 → L3 分数条+实体
6. 页面加载时显示骨架屏脉冲动画，数据到达后替换
7. 事件列表空状态显示引导式组件（含"清除筛选"和"查看诊断"按钮）
8. Tablet (768-1023px)：侧边栏折叠 + 单栏布局 + 2 列统计卡片
9. Mobile (<768px)：侧边栏隐藏 + 全屏布局 + 事件卡片隐藏 L3
10. `ruff check src/news_sentry/` — 0 errors（纯前端改动不影响 Python lint）
11. `.venv/bin/python3 -m pytest tests/ -q` — 1640+ passed
