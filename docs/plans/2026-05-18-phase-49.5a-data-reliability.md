# Phase 49.5a: 数据可靠性 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复自动采集器使其支持多 target 全流程 pipeline，新增采集诊断端点，Dashboard 增加健康卡片和引导式空状态。

**Architecture:** 后端改造集中在 `api_server.py`——将 `_auto_collect_loop` 从单 target collect-only 升级为多 target 可配 stage；新增 `collector/diagnostics` 公开端点。前端改造在 `dashboard.js`——新增健康卡片、零数据引导、诊断入口。

**Tech Stack:** Python 3.11+ / FastAPI / aiosqlite / Vanilla JS ES modules

**Spec:** `docs/specs/2026-05-18-phase-49.5-app-productization-design.md` §3

---

## 文件变更地图

| 文件 | 职责 | 变更类型 |
|------|------|---------|
| `src/news_sentry/core/api_server.py:1037-1092` | 自动采集状态 + 循环 | 修改 — target_id→target_ids, bounded_run_async→bounded_run_multi_async |
| `src/news_sentry/core/api_server.py:1217-1218` | `_data_dir` 初始化 | 修改 — 读取 NEWSSENTRY_DATA_DIR 环境变量 |
| `src/news_sentry/core/api_server.py:1234-1245` | `collector/status` 端点 | 修改 — 返回 target_ids (list) + stage |
| `src/news_sentry/core/api_server.py:1247+` | `collector/diagnostics` 端点 | 新增 — 5 项诊断检查 |
| `src/news_sentry/static/pages/dashboard.js:25-111` | 概览 Tab 渲染 + 数据加载 | 修改 — 健康卡片 + 零数据引导 + 诊断入口 |
| `tests/unit/test_api_server.py` | API 端点测试 | 修改 — 新增 diagnostics + collector_status 测试 |

---

### Task 1: `_auto_collect_loop` 多 target 升级 + `_data_dir` 环境变量

**Files:**
- Modify: `src/news_sentry/core/api_server.py:1037-1092` (auto collector state + loop)
- Modify: `src/news_sentry/core/api_server.py:1217-1218` (_data_dir init)
- Modify: `src/news_sentry/core/api_server.py:1234-1245` (collector/status endpoint)

- [ ] **Step 1: 替换 `_auto_collector_state` 初始化**

将 `api_server.py:1039-1049` 的 `_auto_collector_state` 字典从单 target 改为多 target 列表，新增 `stage` 字段，新增 `_parse_target_ids()` 辅助函数。

在 `_auto_collector_state` 之前插入 `_parse_target_ids`：

```python
def _parse_target_ids(raw: str) -> list[str]:
    """解析 target ID 字符串：'all' → 全量 targets，'a,b' → ['a','b']."""
    if raw.strip().lower() == "all":
        from news_sentry.core.async_run import _resolve_targets
        import news_sentry

        config_dir = Path(news_sentry.__file__).resolve().parent.parent / "config"
        return _resolve_targets("all", config_dir)
    return [t.strip() for t in raw.split(",") if t.strip()]
```

然后替换 `_auto_collector_state`：

```python
_auto_collector_state: dict[str, Any] = {
    "enabled": os.environ.get("NEWSSENTRY_AUTO_COLLECT", "1") == "1",
    "target_ids": _parse_target_ids(
        os.environ.get("NEWSSENTRY_TARGET_ID", os.environ.get("TARGET_ID", "italy"))
    ),
    "interval_minutes": int(os.environ.get("NEWSSENTRY_COLLECT_INTERVAL", "15")),
    "stage": os.environ.get("NEWSSENTRY_COLLECT_STAGE", "collect"),
    "running": False,
    "last_run_at": None,
    "last_run_status": None,
    "last_events_collected": 0,
    "total_runs": 0,
    "task": None,
}
```

- [ ] **Step 2: 替换 `_auto_collect_loop` 函数体**

将 `api_server.py:1054-1091` 的 `_auto_collect_loop` 从调用 `bounded_run_async` 改为 `bounded_run_multi_async`：

```python
async def _auto_collect_loop() -> None:
    """后台循环：每隔 interval_minutes 对每个 target 执行 pipeline 阶段。

    通过 NEWSSENTRY_COLLECT_STAGE 控制执行的阶段（默认 collect），
    通过 NEWSSENTRY_TARGET_ID 控制 target 范围（默认 italy，逗号分隔或 all）。
    """
    interval = _auto_collector_state["interval_minutes"] * 60
    target_ids = _auto_collector_state["target_ids"]
    stage = _auto_collector_state["stage"]
    _auto_collector_state["running"] = True
    _log.info(
        "自动采集循环启动: targets=%s, stage=%s, interval=%dmin",
        target_ids, stage, interval // 60,
    )

    while _auto_collector_state["enabled"]:
        try:
            from news_sentry.core.async_run import bounded_run_multi_async

            run_id = f"auto_{datetime.now(UTC).strftime('%Y%m%dT%H%M%SZ')}"
            _log.info("自动采集开始: run_id=%s, targets=%s", run_id, target_ids)

            await bounded_run_multi_async(
                targets=target_ids,
                stage=stage,
                run_id=run_id,
            )

            _auto_collector_state["last_run_at"] = datetime.now(UTC).isoformat()
            _auto_collector_state["last_run_status"] = "ok"
            _auto_collector_state["total_runs"] += 1
            _log.info("自动采集完成: run_id=%s", run_id)
        except Exception:
            _auto_collector_state["last_run_at"] = datetime.now(UTC).isoformat()
            _auto_collector_state["last_run_status"] = "error"
            _auto_collector_state["total_runs"] += 1
            _log.error("自动采集失败", exc_info=True)

        await asyncio.sleep(interval)

    _auto_collector_state["running"] = False
    _log.info("自动采集循环停止")
```

- [ ] **Step 3: 更新 `collector/status` 端点**

将 `api_server.py:1234-1245` 的返回值从 `target_id` (str) 改为 `target_ids` (list)，新增 `stage`：

```python
@app.get("/api/v1/collector/status")
async def collector_status() -> dict[str, Any]:
    """返回后台自动采集循环的状态。"""
    return {
        "enabled": _auto_collector_state["enabled"],
        "running": _auto_collector_state["running"],
        "target_ids": _auto_collector_state["target_ids"],
        "stage": _auto_collector_state["stage"],
        "interval_minutes": _auto_collector_state["interval_minutes"],
        "last_run_at": _auto_collector_state["last_run_at"],
        "last_run_status": _auto_collector_state["last_run_status"],
        "total_runs": _auto_collector_state["total_runs"],
    }
```

- [ ] **Step 4: 更新 `_data_dir` 读取环境变量**

将 `api_server.py:1217-1218`：

```python
_data_dir = Path(data_dir) if data_dir else Path("./data")
```

改为：

```python
_data_dir = Path(data_dir or os.environ.get("NEWSSENTRY_DATA_DIR", "./data"))
```

- [ ] **Step 5: Lint + 类型检查**

```bash
ruff check src/news_sentry/core/api_server.py
```

Expected: 0 errors.

- [ ] **Step 6: 运行现有测试确保无回归**

```bash
.venv/bin/python3 -m pytest tests/unit/test_api_server.py -q
```

Expected: 所有现有测试通过。

- [ ] **Step 7: Commit**

```bash
git add src/news_sentry/core/api_server.py
git commit -m "feat: _auto_collect_loop 多 target 升级 + _data_dir 环境变量 + collector/status 返回 target_ids"

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

### Task 2: 新增 `collector/diagnostics` 端点 + 测试

**Files:**
- Modify: `src/news_sentry/core/api_server.py` (新增 diagnostics 端点，插在 collector/status 之后)
- Modify: `tests/unit/test_api_server.py` (新增 3 个测试)

- [ ] **Step 1: 在 `api_server.py` 中新增 diagnostics 端点**

在 `collector/status` 端点之后（`api_server.py:1246` 附近）插入：

```python
@app.get("/api/v1/collector/diagnostics")
async def collector_diagnostics() -> dict[str, Any]:
    """返回采集系统诊断信息，帮助排查"无数据"问题。"""
    import json as _json

    checks: list[dict[str, Any]] = []

    # 1. 自动采集是否启用
    checks.append({
        "name": "auto_collect_enabled",
        "ok": _auto_collector_state["enabled"],
        "message": (
            "已启用" if _auto_collector_state["enabled"]
            else "未启用 — 设置 NEWSSENTRY_AUTO_COLLECT=1"
        ),
    })

    # 2. AI API Key 是否配置
    has_ai_key = bool(
        os.environ.get("ANTHROPIC_API_KEY")
        or os.environ.get("OPENAI_API_KEY")
    )
    checks.append({
        "name": "ai_api_key",
        "ok": has_ai_key,
        "message": "已配置" if has_ai_key else "未配置 AI API Key — 研判/翻译将跳过",
    })

    # 3. 数据目录是否存在 + target 目录列表
    data_exists = _data_dir.exists()
    target_dirs = (
        sorted([d.name for d in _data_dir.iterdir() if d.is_dir()])
        if data_exists else []
    )
    checks.append({
        "name": "data_directory",
        "ok": data_exists and len(target_dirs) > 0,
        "message": (
            f"数据目录: {_data_dir} — {len(target_dirs)} 个 target: "
            f"{', '.join(target_dirs) if target_dirs else '无'}"
        ),
    })

    # 4. 信源健康概览
    healthy = 0
    unhealthy = 0
    if data_exists:
        for tid in target_dirs:
            health_file = _data_dir / tid / "source_health.json"
            if health_file.exists():
                try:
                    health_data = _json.loads(health_file.read_text())
                    items = health_data if isinstance(health_data, list) else []
                    for h in items:
                        if h.get("healthy"):
                            healthy += 1
                        else:
                            unhealthy += 1
                except Exception:
                    pass
    checks.append({
        "name": "source_health",
        "ok": (healthy + unhealthy) > 0,
        "message": (
            f"健康: {healthy}, 异常: {unhealthy}"
            if (healthy + unhealthy) > 0
            else "暂无信源健康数据 — 运行一次采集后生成"
        ),
    })

    # 5. 最近一次采集时间
    last_run = _auto_collector_state["last_run_at"]
    checks.append({
        "name": "last_collection",
        "ok": last_run is not None,
        "message": (
            f"最后采集: {last_run}" if last_run
            else "尚未执行采集 — 等待首次采集周期"
        ),
    })

    overall = all(c["ok"] for c in checks)
    return {"overall": "healthy" if overall else "attention_needed", "checks": checks}
```

- [ ] **Step 2: 检查新端点需要的 `json` import**

确认 `api_server.py` 顶部已有 `import json`。查看文件头部 imports：

```bash
grep "^import json" src/news_sentry/core/api_server.py
```

Expected: 匹配到 `import json` 行（或 `import json as ...`）。如不存在，需在文件头部添加 `import json`。

- [ ] **Step 3: 运行 lint**

```bash
ruff check src/news_sentry/core/api_server.py
```

Expected: 0 errors.

- [ ] **Step 4: 在 `test_api_server.py` 中编写 diagnostics 测试**

在 `TestAPIServer` 类的 `test_health_endpoint` 之后新增：

```python
def test_collector_status_returns_target_ids_list(self, tmp_path: Path) -> None:
    """collector/status 返回 target_ids (list) + stage。"""
    client = self._make_client(tmp_path)
    resp = client.get("/api/v1/collector/status")
    assert resp.status_code == 200
    data = resp.json()
    assert "target_ids" in data
    assert isinstance(data["target_ids"], list)
    assert "stage" in data
    assert isinstance(data["stage"], str)

def test_collector_diagnostics_healthy(self, tmp_path: Path) -> None:
    """有数据目录情况下 diagnostics 返回 overall=healthy。"""
    # 创建数据目录和 source_health.json 模拟正常状态
    target_dir = tmp_path / "italy"
    target_dir.mkdir(parents=True, exist_ok=True)
    import json
    (target_dir / "source_health.json").write_text(
        json.dumps([{"source_id": "ansa", "healthy": True}]),
        encoding="utf-8",
    )

    client = self._make_client(tmp_path)
    resp = client.get("/api/v1/collector/diagnostics")
    assert resp.status_code == 200
    data = resp.json()
    assert "overall" in data
    assert "checks" in data
    assert len(data["checks"]) == 5
    # 检查每个 check 的必需字段
    for check in data["checks"]:
        assert "name" in check
        assert "ok" in check
        assert "message" in check
    # source_health 应为 ok（我们创建了 1 条健康记录）
    source_check = [c for c in data["checks"] if c["name"] == "source_health"][0]
    assert source_check["ok"] is True

def test_collector_diagnostics_empty_data_dir(self, tmp_path: Path) -> None:
    """空数据目录下 diagnostics 返回 overall=attention_needed。"""
    client = self._make_client(tmp_path)
    resp = client.get("/api/v1/collector/diagnostics")
    assert resp.status_code == 200
    data = resp.json()
    assert data["overall"] == "attention_needed"
    # data_directory check 应为 not ok
    dd_check = [c for c in data["checks"] if c["name"] == "data_directory"][0]
    assert dd_check["ok"] is False
```

- [ ] **Step 5: 运行新测试验证通过**

```bash
.venv/bin/python3 -m pytest tests/unit/test_api_server.py::TestAPIServer::test_collector_status_returns_target_ids_list tests/unit/test_api_server.py::TestAPIServer::test_collector_diagnostics_healthy tests/unit/test_api_server.py::TestAPIServer::test_collector_diagnostics_empty_data_dir -v
```

Expected: 3 passed.

- [ ] **Step 6: 运行全量后端测试确保无回归**

```bash
.venv/bin/python3 -m pytest tests/unit/test_api_server.py -q
```

Expected: 所有测试通过。

- [ ] **Step 7: Commit**

```bash
git add src/news_sentry/core/api_server.py tests/unit/test_api_server.py
git commit -m "feat: 新增 collector/diagnostics 端点 + 测试"

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

### Task 3: Dashboard 采集健康卡片 + 零数据引导 + 诊断入口

**Files:**
- Modify: `src/news_sentry/static/pages/dashboard.js:25-204` (概览 Tab + 数据加载 + 统计卡片)

- [ ] **Step 1: 零数据引导 — 修改空状态检测逻辑**

当 `statsResp` 为 null 或事件总数为 0 时，当前代码仍显示空列表。需要在 `loadData()` 中检测"完全无数据"情况并渲染引导式空状态。

在 `dashboard.js` 的 `loadData()` 函数中，`renderStatCards` 调用之前插入零数据检测：

```javascript
// 检测完全无数据状态
const totalEvents = statsResp
  ? (statsResp.today_count ?? statsResp.total_events ?? 0)
  : 0;
const hasAnyData = totalEvents > 0 || (eventsResp?.events?.length || eventsResp?.items?.length || 0) > 0;

if (!hasAnyData && collectorResp) {
  // 引导式空状态
  container.innerHTML = `
    <div class="empty-state-guided">
      <div class="empty-state-icon">&#x1F4E1;</div>
      <h2 class="empty-state-title">尚未采集到新闻事件</h2>
      <div class="empty-state-causes">
        <p class="empty-state-label">可能原因：</p>
        <ul>
          <li>自动采集器尚未完成首次运行</li>
          <li>AI API Key 未配置（<a href="#/config/apikey">点击配置</a>）</li>
          <li>信源暂时不可达</li>
        </ul>
      </div>
      <div class="empty-state-actions">
        <button class="btn-secondary" id="btnOpenDiagnostics">查看诊断</button>
        <button class="btn-primary" id="btnManualCollect">手动触发采集</button>
      </div>
    </div>
  `;

  // 诊断按钮
  container.querySelector("#btnOpenDiagnostics").addEventListener("click", async () => {
    try {
      const diagResp = await api("/api/v1/collector/diagnostics");
      showDiagnosticsDialog(diagResp);
    } catch (err) {
      showError(`诊断失败: ${err.message}`);
    }
  });

  // 手动采集按钮（需要 admin 权限）
  container.querySelector("#btnManualCollect").addEventListener("click", async () => {
    try {
      const tid = state.currentTarget || "italy";
      const resp = await api(`/api/v1/runs/trigger?target_id=${encodeURIComponent(tid)}&stage=all`, null, "POST");
      showSuccess(`采集已触发: ${resp.run_id || "ok"}`);
      // 等待 3 秒后重新加载
      setTimeout(() => loadData(container), 3000);
    } catch (err) {
      showError(`触发采集失败: ${err.message}`);
    }
  });

  return;
}
```

- [ ] **Step 2: 渲染"采集健康"卡片**

在 `renderStatCards()` 函数末尾，系统状态卡片之后追加健康卡片渲染逻辑。修改现有的第 4 张卡片 (statStatus)，加入采集器状态摘要和健康入口：

将 `renderStatCards` 中卡片 4（`statStatus`）的渲染逻辑（约第 194-203 行）替换为：

```javascript
// 卡片 4: 系统状态 + 采集器心跳 + 健康入口
if (collector) {
  const running = collector.running;
  const enabled = collector.enabled;
  let statusText, statusColor;
  if (running) {
    statusText = "运行中";
    statusColor = "var(--accent-green)";
  } else if (enabled) {
    statusText = "等待中";
    statusColor = "var(--accent-yellow, #eab308)";
  } else {
    statusText = "已停用";
    statusColor = "var(--text-muted)";
  }
  el("statStatus").textContent = statusText;
  el("statStatus").style.color = statusColor;

  const lastRun = collector.last_run_at;
  const targets = collector.target_ids || [];
  el("statHeartbeat").innerHTML = `
    ${lastRun ? `上次采集: ${formatDate(lastRun)}` : ""}
    <br><small>${targets.length} 个目标</small>
    <br><a href="javascript:void(0)" class="diagnostics-link" id="dashboardDiagLink">查看诊断 →</a>
  `;

  // 诊断入口
  const diagLink = container.querySelector("#dashboardDiagLink");
  if (diagLink) {
    diagLink.addEventListener("click", async (e) => {
      e.preventDefault();
      try {
        const diagResp = await api("/api/v1/collector/diagnostics");
        showDiagnosticsDialog(diagResp);
      } catch (err) {
        showError(`诊断失败: ${err.message}`);
      }
    });
  }
} else {
  el("statStatus").textContent = "—";
  el("statHeartbeat").textContent = "采集器状态未知";
}
```

- [ ] **Step 3: 添加诊断弹窗函数**

在 `dashboard.js` 文件末尾（`handleExportBriefing` 之后）新增诊断弹窗函数：

```javascript
/**
 * 显示采集诊断弹窗。
 * @param {object} diagResp — /api/v1/collector/diagnostics 返回的数据
 */
function showDiagnosticsDialog(diagResp) {
  const checks = diagResp?.checks || [];
  const overall = diagResp?.overall || "unknown";
  const overallIcon = overall === "healthy" ? "&#x2705;" : "&#x26A0;&#xFE0F;";
  const overallLabel = overall === "healthy" ? "系统状态: 正常" : "系统状态: 需要关注";

  const rows = checks.map((c) => {
    const icon = c.ok ? "&#x2705;" : "&#x274C;";
    return `<tr>
      <td>${icon}</td>
      <td><strong>${escapeHtml(c.name)}</strong></td>
      <td style="font-size:0.85rem;color:var(--text-muted)">${escapeHtml(c.message)}</td>
    </tr>`;
  }).join("");

  const dialog = document.createElement("div");
  dialog.className = "modal-overlay";
  dialog.innerHTML = `
    <div class="modal-content" style="max-width:640px">
      <div class="modal-header">
        <span>${overallIcon} ${overallLabel}</span>
        <button class="modal-close">&times;</button>
      </div>
      <table class="diagnostics-table">
        <tbody>${rows}</tbody>
      </table>
    </div>
  `;

  dialog.querySelector(".modal-close").addEventListener("click", () => dialog.remove());
  dialog.addEventListener("click", (e) => { if (e.target === dialog) dialog.remove(); });

  document.body.appendChild(dialog);
}
```

- [ ] **Step 4: 在 `style.css` 中添加空状态引导和诊断弹窗样式**

在 `src/news_sentry/static/style.css` 末尾新增以下样式：

```css
/* ── 引导式空状态 ─────────────────────── */

.empty-state-guided {
  text-align: center;
  padding: 3rem 2rem;
  max-width: 480px;
  margin: 2rem auto;
}

.empty-state-icon {
  font-size: 3rem;
  margin-bottom: 1rem;
}

.empty-state-title {
  font-size: 1.25rem;
  font-weight: 600;
  color: var(--text-title, #f0f1f5);
  margin-bottom: 1rem;
}

.empty-state-causes {
  text-align: left;
  color: var(--text-muted);
  font-size: 0.875rem;
  margin-bottom: 1.5rem;
}

.empty-state-label {
  font-weight: 500;
  color: var(--text-primary);
  margin-bottom: 0.5rem;
}

.empty-state-causes ul {
  padding-left: 1.25rem;
}

.empty-state-causes li {
  margin-bottom: 0.25rem;
}

.empty-state-actions {
  display: flex;
  gap: 0.75rem;
  justify-content: center;
}

/* ── 诊断弹窗表格 ─────────────────────── */

.diagnostics-table {
  width: 100%;
  border-collapse: collapse;
}

.diagnostics-table td {
  padding: 0.5rem 0.5rem;
  vertical-align: top;
  border-bottom: 1px solid var(--border-color, #1f2335);
}

.diagnostics-table td:first-child {
  width: 28px;
  text-align: center;
}

.diagnostics-link {
  color: var(--accent-blue);
  text-decoration: none;
  font-size: 0.8rem;
}

.diagnostics-link:hover {
  text-decoration: underline;
}
```

- [ ] **Step 5: 验证前端无 JS 语法错误**

```bash
node -e "import('./src/news_sentry/static/pages/dashboard.js').catch(e => { if (e.message.includes('SyntaxError')) { console.error(e.message); process.exit(1) } else { console.log('Import skipped (expected in browser context)') } })" 2>&1
```

Expected: 无 SyntaxError 输出。

- [ ] **Step 6: 运行全量测试确保无回归**

```bash
.venv/bin/python3 -m pytest tests/ -q 2>&1 | tail -5
```

Expected: 1640+ tests passed.

- [ ] **Step 7: Commit**

```bash
git add src/news_sentry/static/pages/dashboard.js src/news_sentry/static/style.css
git commit -m "feat: Dashboard 零数据引导 + 采集诊断弹窗 + 健康卡片增强"

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

## 验证清单

完成全部 3 个 Task 后执行：

1. `GET /api/v1/health` → `{"status": "ok"}`
2. `GET /api/v1/collector/status` → 返回 `target_ids` (list) + `stage` + `enabled` 等字段
3. `GET /api/v1/collector/diagnostics` → 返回 `{"overall": "...", "checks": [...]}` 含 5 项检查
4. Dashboard 在无数据时显示引导式空状态（含"查看诊断"和"手动触发采集"按钮）
5. Dashboard 在有数据时显示"查看诊断 →"链接
6. 点击诊断入口弹出诊断弹窗，显示 5 项检查结果
7. `ruff check src/news_sentry/core/api_server.py` — 0 errors
8. `.venv/bin/python3 -m pytest tests/ -q` — 1640+ passed
