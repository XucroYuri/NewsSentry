.DS_Store
**/.DS_Store
.claude/
CLAUDE.local.md
.cursor/
CLAUDE-PROMPT.md
.env
.env.*
!.env.example
*.log

# Python build artifacts
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
*.cover

# Legacy pre-profile runtime root. Runtime data now belongs under data/{target_id}/.
/italy/

# Config secrets (env-var placeholders are fine, actual secrets are not)
config/provider/prompts/*.actual.yaml

# Package manager lock files
uv.lock

# OMC plugin state (session-local, not project code)
.omc/

# Git worktrees (isolated workspaces)
.worktrees/

# Runtime data (generated by bounded_run, tests)
data/
# But track eval set (core benchmark asset)
!data/eval/eval-set-v1.json

# IDE/toolchain local config (not project code)
.codex/

# Development planning workspace (local only, v1.0+ reevaluate)
.planning/

# Local document archive (dev-reference docs not for publication)
docs/_archive/
config/**/*.local.yaml
config/**/*.local.yml

# Chrome / browser profiles (Phase 6)
**/Chrome/
**/chromium/
**/Cookies
**/Login Data
**/chrome-profiles/
**/session-profiles/*.actual.yaml
