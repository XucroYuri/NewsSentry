# =============================================================================
# News Sentry — 环境变量模板
# =============================================================================
# 复制此文件为 .env 并填入实际值:
#   cp .env.example .env
#
# .env 已被 .gitignore 忽略，不会提交到仓库。
# =============================================================================

# ── 必填 ─────────────────────────────────────────────────────────────────────

# 部署 profile: cloud-vps (生产) | local-workstation (开发/测试)
NEWSSENTRY_PROFILE=local-workstation

# 数据输出根目录（相对于项目根）
# 默认: ./data
# NEWSSENTRY_DATA_DIR=./data

# ── AI Provider (Phase 5 需要; Phase 3 规则引擎不需要) ─────────────────────

# DeepSeek API (当前测试用)
# DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx

# OpenAI API (备用)
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxx

# Anthropic API (备用)
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxx

# ── 推送通知 (可选) ─────────────────────────────────────────────────────────

# 飞书 Webhook（高价值 Breaking News 推送）
# 获取方式: 飞书群 → 群设置 → 群机器人 → 添加 → Webhook
# FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx

# Telegram Bot (需配合 Hermes Gateway 使用)
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234gh

# ── 代理 (中国大陆环境必需) ──────────────────────────────────────────────────

# 全协议 SOCKS5 代理
# all_proxy=socks5://127.0.0.1:1080

# HTTP/HTTPS 代理
# http_proxy=http://127.0.0.1:7890
# https_proxy=http://127.0.0.1:7890

# ── 高级选项 ─────────────────────────────────────────────────────────────────

# 项目根目录覆盖（通常不需要设置）
# NEWSSENTRY_PROJECT_ROOT=/path/to/NewsSentry

# 允许输出到项目外目录（安全开关，默认关闭）
# NEWSSENTRY_ALLOW_EXTERNAL_OUTPUT=0

# 日志级别: DEBUG | INFO | WARNING (默认 WARNING)
# NEWSSENTRY_LOG_LEVEL=INFO
